* Scan: load playlists in table
* get image for stream
  > mopidy.get_images(uris) returns {[string: string]: Image[]}
  > backend should return the radio stream's image
    > stored in root.eboplayer -> "streams_folder" -> streams.eboplayer.playlist

* list all the playlist files
   > add them to the playlists table.

* Scan: check for new image files
  > this implies that the original file names are stored in the DB.


ROOT META
===========
whole of setup should be able to be recreated from files.
Thus the root meta should contain:
  - general settings for this media library.
    > assume that eboplayer will be able to have multiple media libraries (disks,...)
    - Genre aliasses
    - ...
  - playlists (overview) ?
  - ...
  > load root meta at startup
    > no need for sql storage.
    > load playlists into sql
      > load the actual playlists from file (on demand...or stored in mem?)

PLAYLISTS
=========
* harvest wpl files.
  > part of meta command or separate playlists command?
  > mark them as wpl playlists?
* create a playlists table(s)
  - name
  - filename
  - retrieve them 'live' for now.


* create complex playlists
  - nested
  - filter based

* Figure out how to get album info to the client(s).
  > search for albums should ALWAYS return album refs (or uri's)
  > options:
    > Ref objects that are returned are NOT opaque and contain extra info.
      > either via an extra field, but that's probably illegal in mopidy.
      > or added to the uri field
    > via www -> GET:
      > after all, it doesn't require events (push messaging).

